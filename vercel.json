{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "${VITE_API_URL}/:path*"
    },
    {
      "source": "/api/:path*/",
      "destination": "${VITE_API_URL}/:path*/"
    },
    { "source": "/(.*)", "destination": "/" }
  ]
}
